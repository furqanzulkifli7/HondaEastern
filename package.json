{
  "name": "honda-eastern",
  "version": "1.0.0",
  "private": true,
  "description": "Honda Eastern static site",
  "scripts": {
    "dev": "live-server --port=3000 --no-browser --wait=200",
    "start": "live-server --port=3000 --wait=200"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
